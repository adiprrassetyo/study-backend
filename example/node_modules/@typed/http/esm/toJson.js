import { chain, tryCatch } from '@typed/either';
export function toJson(response) {
    return chain(({ responseText }) => tryCatch(() => JSON.parse(responseText)), response);
}
//# sourceMappingURL=toJson.js.map