import { curry } from '@typed/lambda';
export const addHeader = curry((name, value, { http }) => {
    return {
        http: (url, options, callbacks) => http(url, { ...options, headers: { ...options.headers, [name]: value } }, callbacks),
    };
});
export const addAuthorizationHeader = addHeader('Authorization');
//# sourceMappingURL=addHeader.js.map